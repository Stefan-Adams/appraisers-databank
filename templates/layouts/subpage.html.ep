<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title><%= title %></title>
        %= asset 'adb.css'
        <meta name="viewport" content="width=device-width">
    </head>
    <body>
        <div class="int header">
            <div class="flex container">
                <h1 class="title"><%= link_to "Appraiser's DataBank" => 'home' %></h1>
                    <ul class="logged-out-nav nav">
                        %= include 'snippets/topmenu'
                    </ul>
            </div>
        </div>
        <%= stash 'success' %><%= stash 'error' %><%= flash 'f_success' %><%= flash 'f_error' %><br />
        <div class="content-area">
            <%= content %>
        </div>
        <div class="footer">
           <div class="container">
                <ul class="footer-nav">
                    %= include 'snippets/bottommenu'
                </ul>
           </div>
        </div>
    </body>
</html>